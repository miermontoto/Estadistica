
load("G:/OneDrive - Universidad de Oviedo/Uni/Y1T2/Estadística/Estad-stica_R/acero.rda")
# Cola izquierda P(x <= 7)
# Cola derecha   P(x > 7)

# P(x<=a) = p
> Se da 'p' y se pide 'a': Cuantiles
> Se da 'a' y se pide 'p': Probabilidades acumuladas

# P(x=a) = p
> Se da 'a' y se pide 'p': Probabilidades

 # MUCHO CUIDADO CON LAS COLAS son unas hijas de puta de la hostia
 # p(4 <= X <= 7) = p(x <= 7) - p(x <= 3) <-- CUIDADO!!! 
 # p(7 < X) = p(6 <= X) <-- CUIDADO!!!

# Modelos distribuciones discretas
<> Distribuciones -> Distribuciones discretas

# Modelos distribuciones continuas
<> Distribuciones -> Distribuciones continuas

> Generar un conjunto de datos a partir de una muestra aleatoria de 10000 observaciones de una distribución continua
<> Distribuciones -> Distribuciones continuas -> Distribución (?) -> Muestra de una distribución normal
